html
  head
    title="Home"
  body
    h1 Welcome to the SOLID-if-IoT Steward App.
    form(action="/login" method="post" id="oidc" enctype="multipart/form-data")
      label(for="oidcIssuer") OIDC Issuer:
      input(name="oidcIssuer" type="text" id="oidcIssuer")
      input(type="submit" value="Login to OIDC Provider")
    
    